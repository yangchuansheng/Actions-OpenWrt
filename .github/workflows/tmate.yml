# .github/workflows/my-build.yml
name: tmate
on: 
  watch:
    types: started
jobs:
  build:
    runs-on: macOS Catalina 10.15
    steps:
     - uses: actions/checkout@v2

     - name: Setup Debug Session
       uses: csexton/debugger-action@master
